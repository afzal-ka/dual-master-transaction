# dual-master-transaction
A simple Spring Boot + Thymeleaf application that loads and transfers data from two Master tables into a single Transaction table. The project demonstrates a clean UI using Thymeleaf and basic backend logic for selecting records from Master tables and loading them into the Transaction table.
